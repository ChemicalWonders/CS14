#include <iostream>

using namespace std;

//Problem #2: Swap two adjacent elements of a singly linked list

struct Node
{
    int data; 
    //Using integers as the data type, this can be swapped with anything
    
    Node *next;
    Node (int data) : data(data), next(0) {}
};
